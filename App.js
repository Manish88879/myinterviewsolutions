import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App'; // Assuming App.js is located inside src directory

// Register the main component
AppRegistry.registerComponent('myinterviewsolution', () => App);